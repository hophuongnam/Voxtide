/target
**/*.rs.bk
*.rlib
.DS_Store
.env
.env.local
signing.key
node_modules/
dist/
.svelte-kit/

# Tauri 2 regenerates these from capabilities/ on every build (acl-manifests.json + *-schema.json)
/src-tauri/gen/schemas/

# Implementation plans live alongside specs but are convention-tracked locally only —
# only docs/superpowers/specs/ is committed. Keeps the tree clean for release.sh's pre-flight.
/docs/superpowers/plans/

# Subagent-driven-development execution ledger — local scratch, never committed.
/.superpowers/
